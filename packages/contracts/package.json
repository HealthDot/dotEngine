{
  "name": "@healthdot/contracts",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "compile": "cd patient && cargo compile --verbose",
    "build": "cd patient && cargo build --verbose",
    "test": "cd patient && cargo test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
